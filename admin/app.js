/* MOVED / DEAD — do not use, do not <script src> this file.
 *
 * This was a stale, pre-security-fix snapshot of ../js/app.js — it
 * predates the escapeHtml() fix for stored XSS via team names, and also
 * predates the later fixes for unescaped admin-editable fields (tournament
 * name, YouTube video ID, room details, match info, prize pool currency/
 * distribution labels) rendered on the public site. It is not currently
 * loaded by any HTML page in this repo (verified: no admin/*.html
 * references it) — but if it ever is, every one of those bugs comes back
 * with no warning.
 *
 * If an admin-side landing/ticker page is genuinely needed, load
 * ../js/app.js directly, or copy from it fresh — don't resurrect this file.
 */
